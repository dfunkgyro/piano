# Release Notes: stable-2026-04-08-0208

Tag: `release/piano-stable-2026-04-08`
Source baseline tag: `source-baseline-2026-04-08`

## Highlights
- Established immutable S3 rollback snapshots for stable builds.
- Added one-command stable release workflow via `tools/release_stable.ps1`.
- Added one-command rollback workflow via `tools/rollback_release.ps1`.
- Imported the full GrandPiano source into the GitHub repository.
- Replaced visible Flutter default branding/icons with `assets/icon/icon.png` in app and bridge outputs.

## Protected release artifacts
- Web app build
- Main Android APK
- Bridge Android APK
- Bridge Windows ZIP
- Bridge manifest
- Song catalog
- SHA-256 artifact hashes and release manifest

## Rollback
```powershell
powershell -ExecutionPolicy Bypass -File tools\rollback_release.ps1 -ReleaseId stable-2026-04-08-0208
```

## Notes
- This release is also stored in S3 under `releases/stable-2026-04-08-0208`.
- If a hosted GitHub Release object is needed later, it can be created from this note once GitHub CLI or API credentials are available.
